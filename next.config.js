/** @type {import('next').NextConfig} */
const nextConfig = {
  /* ── Image domains ───────────────────────────────────────
     Add client image CDN domains here as needed.           */
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
